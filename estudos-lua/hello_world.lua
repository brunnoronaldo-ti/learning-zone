local nome = "lua test"     -- String
local versao = 2.0          -- Number (não separa int de float)
local status = true         -- Boolean
local nada = nil            -- O "None" ou "null"

-- Unlike Python (dynamic typing) and JS (let/const), in Lua we use local. Always use local, or the variable becomes global and goes out of scope.